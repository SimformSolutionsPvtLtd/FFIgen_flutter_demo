name: 'AudioPlayerUtils'
language: objc

output: lib/audio_player.dart

headers:
  entry-points:
    - 'ios_headers/audioplayer.h'
  include-directives:
    - 'ios_headers/audioplayer.h'

exclude-all-by-default: true

objc-interfaces:
  include:
    - AudioPlayer
  #module:
  #  'AudioPlayer': 'audioplayer'